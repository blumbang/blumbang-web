exports.handler = async function() {
  const API = 'https://blumbang-data-api.blmbng-id.workers.dev/?sheet=kompetitor&tab=Data%20Kompetitor';
  
  try {
    const res = await fetch(API);
    const data = await res.text();
    
    return {
      statusCode: 200,
      headers: { 'Content-Type': 'application/json' },
      body: data
    };
  } catch(e) {
    return {
      statusCode: 500,
      body: JSON.stringify({ error: e.toString() })
    };
  }
};
```

Commit. Tunggu Netlify deploy.

Setelah deploy — endpoint-nya jadi:
```
https://blumbang.id/.netlify/functions/tor
